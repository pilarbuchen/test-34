import { createBoard } from '@wixc3/react-board';
import { NewComponentcc } from '../../../src/components/new-componentcc/new-componentcc';

export default createBoard({
    name: 'NewComponentcc',
    Board: () => <NewComponentcc />,
});
